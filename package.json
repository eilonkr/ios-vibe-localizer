{
  "name": "ios-localizer-action",
  "version": "0.1.0",
  "description": "A GitHub Action to print a test message.",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/main.ts -o dist --source-map --license licenses.txt",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github",
    "actions",
    "ios",
    "localization"
  ],
  "author": "eilonkr",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@actions/exec": "^1.1.1"
  },
  "devDependencies": {
    "typescript": "^5.0.4",
    "@vercel/ncc": "^0.38.1",
    "@types/node": "^20.12.2"
  }
} 